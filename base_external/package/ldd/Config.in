
config BR2_PACKAGE_LDD
	bool "ldd"
	help
	  Includes the misc-modules and scull components into the root filesystem and adds them in the kernel image.
	  Used to understand buildroot external trees and package builds.
